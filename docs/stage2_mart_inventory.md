# Stage 2 Mart Inventory

Fourteen canonical BI marts at contract version `1.0.0` are exported from
accepted Stage 1 build `20260807T121247Z`. The machine-readable inventory,
row counts, columns, lineage, and SHA-256 checksums are in
`data/bi/20260807T121247Z/manifest.json`. Stage 1 is immutable; all new
outputs are isolated under `data/bi/`.
